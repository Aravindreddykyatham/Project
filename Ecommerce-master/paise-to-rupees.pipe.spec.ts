import { PaiseToRupeesPipe } from './paise-to-rupees.pipe';

describe('PaiseToRupeesPipe', () => {
  it('create an instance', () => {
    const pipe = new PaiseToRupeesPipe();
    expect(pipe).toBeTruthy();
  });
});
